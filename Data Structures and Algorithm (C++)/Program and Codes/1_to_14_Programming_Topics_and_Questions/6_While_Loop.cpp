# include<iostream>

using namespace std;

int main(){
    #ifndef ONLINE_JUDGE
        freopen("Input.txt", "r", stdin);
        freopen("Output.txt", "w", stdout);
    #endif

    int n;
    cin>>n;

    while(n>0){
        cout<<n<<endl;
        cin>>n;
    }

// This program will print the all the +ve no given as input, will terminate when a -ve no is provided.

}
